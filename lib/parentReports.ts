import {sql,withDbRetry} from '@/lib/db';
export async function generateWeeklyReport(parentId:string,studentId:string,weekStart:string,weekEnd:string){const [child]=await withDbRetry(()=>sql`SELECT full_name FROM users WHERE id=${studentId}`);if(!child)return null;const [lesson]=await withDbRetry(()=>sql`SELECT COUNT(*) FILTER(WHERE coverage_complete)::int completed FROM tutor_topic_progress WHERE student_id=${studentId} AND updated_at>=${weekStart}::date AND updated_at<(${weekEnd}::date+interval '1 day')`);const [attempt]=await withDbRetry(()=>sql`SELECT COUNT(*)::int attempts,COUNT(*) FILTER(WHERE is_correct)::int correct FROM attempts WHERE student_id=${studentId} AND created_at>=${weekStart}::date AND created_at<(${weekEnd}::date+interval '1 day')`);const [live]=await withDbRetry(()=>sql`SELECT COUNT(*)::int n,ROUND(AVG(overall_percent))::int avg,(array_agg(strongest_topic ORDER BY overall_percent DESC) FILTER(WHERE strongest_topic IS NOT NULL))[1] strongest,(array_agg(weakest_topic ORDER BY overall_percent ASC) FILTER(WHERE weakest_topic IS NOT NULL))[1] weakest,(array_agg(recommended_topic ORDER BY generated_at DESC) FILTER(WHERE recommended_topic IS NOT NULL))[1] recommended FROM live_assessment_reports WHERE student_id=${studentId} AND generated_at>=${weekStart}::date AND generated_at<(${weekEnd}::date+interval '1 day')`);const [m]=await withDbRetry(()=>sql`SELECT ROUND(AVG(score)*100)::int avg FROM mastery WHERE student_id=${studentId}`);const accuracy=Number(attempt?.attempts||0)?Math.round(Number(attempt.correct||0)/Number(attempt.attempts)*100):null;const summary=`${child.full_name} completed ${lesson?.completed||0} Tutor topic${Number(lesson?.completed||0)===1?'':'s'} and made ${attempt?.attempts||0} independent attempt${Number(attempt?.attempts||0)===1?'':'s'} this week${accuracy==null?'.':` at ${accuracy}% accuracy.`}${live?.n?` Parent-supervised assessment average: ${live.avg}%.`:''}${live?.recommended?` Recommended next focus: ${live.recommended}.`:''}`;const [r]=await withDbRetry(()=>sql`INSERT INTO parent_weekly_reports(parent_id,student_id,week_start,week_end,lessons_completed,attempts,correct_answers,supervised_assessments,supervised_average,mastery_average,strongest_topic,weakest_topic,recommended_next,summary) VALUES(${parentId},${studentId},${weekStart},${weekEnd},${lesson?.completed||0},${attempt?.attempts||0},${attempt?.correct||0},${live?.n||0},${live?.avg??null},${m?.avg??null},${live?.strongest??null},${live?.weakest??null},${live?.recommended??null},${summary}) ON CONFLICT(parent_id,student_id,week_start) DO UPDATE SET week_end=EXCLUDED.week_end,lessons_completed=EXCLUDED.lessons_completed,attempts=EXCLUDED.attempts,correct_answers=EXCLUDED.correct_answers,supervised_assessments=EXCLUDED.supervised_assessments,supervised_average=EXCLUDED.supervised_average,mastery_average=EXCLUDED.mastery_average,strongest_topic=EXCLUDED.strongest_topic,weakest_topic=EXCLUDED.weakest_topic,recommended_next=EXCLUDED.recommended_next,summary=EXCLUDED.summary,generated_at=now() RETURNING *`);await withDbRetry(()=>sql`INSERT INTO parent_notifications(parent_id,student_id,kind,title,body,href) VALUES(${parentId},${studentId},'WEEKLY_REPORT',${`${child.full_name}'s weekly learning report`},${summary},${`/parent/child/${studentId}`})`);return r}
