import {NextResponse} from 'next/server';import {getSession} from '@/lib/auth';import {sql,withDbRetry} from '@/lib/db';import {z} from 'zod';
const schema=z.object({sessionId:z.string().uuid(),questionId:z.string().uuid(),answer:z.string().max(500)});
export async function POST(req:Request){const s=await getSession();if(!s)return NextResponse.json({error:'Please sign in again.'},{status:401});try{const d=schema.parse(await req.json());const rows=await withDbRetry(()=>sql`UPDATE exam_sessions SET answers=answers||${sql.json({[d.questionId]:d.answer})} WHERE id=${d.sessionId} AND student_id=${s.userId} AND status='ACTIVE' RETURNING id`);if(!rows.length)return NextResponse.json({error:'This assessment is no longer active.'},{status:409});return NextResponse.json({ok:true})}catch(e){console.error('exam answer',e);return NextResponse.json({error:'Your answer could not sync yet. It remains on this screen — retry before submitting.'},{status:503})}}
