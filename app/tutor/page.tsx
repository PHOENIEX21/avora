import {getSession} from '@/lib/auth';import {redirect} from 'next/navigation';import {Suspense} from 'react';import TutorClient from '@/components/TutorClient';
export default async function Tutor(){if(!await getSession())redirect('/login');return <main className="tutor-page shell"><header className="tutor-premium-head"><div><span className="section-kicker">AVORA PERSONAL TUTOR</span><h1>A tutor that stays with you until it makes sense.</h1></div><p>AVORA begins by checking what you already understand, explains only what is missing, guides you through one problem, then asks you to prove the skill without help. You can always choose the topic.</p></header><Suspense fallback={<section className="tutor-loading"><b>AVORA is preparing your lesson…</b></section>}><TutorClient/></Suspense></main>}
