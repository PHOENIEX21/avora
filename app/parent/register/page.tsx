import {redirect} from 'next/navigation';import {getSession} from '@/lib/auth';import ParentRegisterForm from '@/components/ParentRegisterForm';
export default async function Page(){const s=await getSession();if(s?.role==='PARENT')redirect('/parent');if(s?.role==='ADMIN')redirect('/admin');return <main className="parent-auth-page"><div className="shell parent-auth-layout"><section><span className="section-kicker">AVORA FOR FAMILIES</span><h2>Create the parent account separately.</h2><p>You are currently signed in as a learner. This form creates a separate Parent account; it does not replace or duplicate your learner account. After registration, AVORA opens Family setup so you can connect this learner with the one-time code or create a new learner profile.</p><div className="parent-auth-family-note"><b>What happens next</b><span>1. Create parent account · 2. Open Family setup · 3. Enter the learner’s code · 4. The learner appears in the parent dashboard.</span></div><div className="parent-auth-points"><div><b>01</b><span><strong>Separate identities</strong><small>The child keeps their own login, class, Tutor history and progress.</small></span></div><div><b>02</b><span><strong>One-time connection code</strong><small>The learner generates it from Parent connection. It expires and can be used once.</small></span></div><div><b>03</b><span><strong>Family seats</strong><small>The parent can connect up to three separate learner profiles.</small></span></div></div></section><ParentRegisterForm/></div></main>}
