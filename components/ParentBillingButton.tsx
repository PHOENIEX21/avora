'use client';import {useState} from 'react';
export default function ParentBillingButton(){const[busy,setBusy]=useState(false),[msg,setMsg]=useState('');async function go(){setBusy(true);setMsg('');try{const r=await fetch('/api/billing/paystack/initialize',{method:'POST'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not start checkout.');window.location.assign(d.authorizationUrl)}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}return <div className="parent-billing-action"><button onClick={go} disabled={busy}>{busy?'Opening secure checkout…':'Upgrade now to Premium'}</button>{msg&&<p>{msg}</p>}</div>}
