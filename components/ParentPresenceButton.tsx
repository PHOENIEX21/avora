'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function ParentPresenceButton({assessmentId,studentId,confirmed}:{assessmentId:string;studentId:string;confirmed:boolean}){const [busy,setBusy]=useState(false),[error,setError]=useState('');const r=useRouter();async function confirm(){setBusy(true);setError('');const res=await fetch(`/api/parent/live-assessment/${assessmentId}/confirm`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({studentId})});const d=await res.json();setBusy(false);if(!res.ok){setError(d.error||'Could not confirm presence.');return}r.refresh()}if(confirmed)return <div className="parent-confirmed-badge"><b>✓ Presence confirmed</b><span>Your authenticated parent account is recorded for this session.</span></div>;return <div><button type="button" className="premium-primary" onClick={confirm} disabled={busy}>{busy?'Confirming…':'I am here supervising →'}</button>{error&&<p className="form-error">{error}</p>}</div>}
