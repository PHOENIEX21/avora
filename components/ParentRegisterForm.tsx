'use client';
import {FormEvent,useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';
export default function ParentRegisterForm(){const router=useRouter();const [busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);const r=await fetch('/api/parent/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({fullName:f.get('fullName'),email:f.get('email'),phone:f.get('phone'),password:f.get('password')})});const d=await r.json();if(!r.ok){setBusy(false);setError(d.error||'Could not create account.');return}router.replace(d.next||'/parent/family');router.refresh()}return <form onSubmit={submit} className="parent-auth-card"><span>PARENT / GUARDIAN</span><h1>Create your family account.</h1><p>After this step you will go to <b>Family setup</b>. There you either enter the code from the learner’s Parent connection page or create a brand-new learner account.</p><label>Full name<input name="fullName" minLength={2} maxLength={100} required autoComplete="name"/></label><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Phone <small>optional</small><input name="phone" maxLength={40} autoComplete="tel"/></label><label>Password<input name="password" type="password" minLength={8} maxLength={128} required autoComplete="new-password"/></label>{error&&<p className="form-error">{error}</p>}<button className="premium-primary" disabled={busy}>{busy?'Creating family account…':'Create parent account & continue →'}</button><p className="parent-auth-foot">Already registered? <Link href="/login">Sign in</Link></p></form>}
