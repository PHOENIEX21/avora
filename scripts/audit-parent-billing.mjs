import fs from 'node:fs';import path from 'node:path';
const root=process.cwd();const need=['database/migrations/019_parent_reports_billing_paystack.sql','lib/billing.ts','lib/parentReports.ts','app/parent/billing/page.tsx','app/api/billing/paystack/initialize/route.ts','app/api/billing/paystack/verify/route.ts','app/api/billing/paystack/webhook/route.ts'];let pass=0;for(const f of need){if(fs.existsSync(path.join(root,f)))pass++;else console.error('Missing',f)}const migration=fs.readFileSync(path.join(root,need[0]),'utf8');const checks=['billing_accounts','billing_student_seats','billing_transactions','parent_weekly_reports','parent_notifications','max_students'];for(const c of checks){if(migration.includes(c))pass++;else console.error('Missing schema marker',c)}const webhook=fs.readFileSync(path.join(root,'app/api/billing/paystack/webhook/route.ts'),'utf8');if(webhook.includes('sha512')&&webhook.includes('x-paystack-signature'))pass++;else console.error('Webhook signature verification missing');const expected=need.length+checks.length+1;console.log(`Parent billing/report audit: ${pass}/${expected}`);if(pass!==expected)process.exit(1);
